import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.ev1',
  appName: 'ev1',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
